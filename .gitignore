# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (nunca commitar credenciais reais)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Claude Code local-machine settings/state (not shared) — a pasta inteira, não só os dois
# arquivos que já estavam aqui: settings.json chegou a ser versionado com credenciais reais
# de teste (ver auditoria de segurança) e não deveria nunca ter ido pro repositório público.
.claude/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

CLAUDE.md
README.md

# Supabase CLI local cache (link do projeto, versões, pooler URL) — estado da máquina, não
# do projeto; nunca deveria estar num repositório público (revela endpoint/usuário do pooler).
supabase/.temp/
